import { motion } from 'motion/react';
import { ArrowRight, Calendar, MapPin, Sparkles } from 'lucide-react';
import { Button } from '@/components/ui/button';

export default function HomePage() {
  return (
    <div className="min-h-screen" style={{ paddingTop: '117px' }}>
      {/* Hero Section with Parallax */}
      <section className="relative h-screen overflow-hidden">
        <motion.div
          className="absolute inset-0"
          initial={{ scale: 1.1 }}
          animate={{ scale: 1 }}
          transition={{ duration: 1.5, ease: 'easeOut' }}
        >
          <img
            src="/airo-assets/images/pages/home/hero"
            alt="Luxury Indian Wedding"
            className="h-full w-full object-cover"
          />
          <div className="absolute inset-0 bg-gradient-to-r from-gray-900/70 via-gray-900/50 to-transparent" />
        </motion.div>

        <div className="relative z-10 flex h-full items-center">
          <div className="container mx-auto px-6 lg:px-12">
            <motion.div
              className="max-w-2xl"
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.3 }}
            >
              <h1 className="mb-6 font-heading text-5xl font-light tracking-wide text-white lg:text-7xl">
                Crafting Unforgettable
                <span className="mt-2 block font-normal">Celebrations</span>
              </h1>
              <p className="mb-8 text-lg text-white/90 lg:text-xl">
                Luxury wedding planning for discerning couples across India and beyond
              </p>
              <Button
                size="lg"
                className="group bg-primary text-primary-foreground hover:bg-primary/90"
              >
                Begin Your Journey
                <ArrowRight className="ml-2 h-5 w-5 transition-transform group-hover:translate-x-1" />
              </Button>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Services Section - Editorial Layout */}
      <section className="py-24 lg:py-32">
        <div className="container mx-auto px-6 lg:px-12">
          <motion.div
            className="mb-16 text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <h2 className="mb-4 font-heading text-4xl font-light tracking-wide lg:text-5xl">
              Our Services
            </h2>
            <p className="mx-auto max-w-2xl text-lg text-muted-foreground">
              From intimate ceremonies to grand celebrations, we orchestrate every detail with precision and elegance
            </p>
          </motion.div>

          {/* Service 1 - Full Planning */}
          <motion.div
            className="mb-24 grid items-center gap-12 lg:grid-cols-2"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <div className="order-2 lg:order-1">
              <div className="flex items-center gap-3 mb-4">
                <Sparkles className="h-6 w-6 text-primary" />
                <h3 className="font-heading text-3xl font-light tracking-wide">
                  Complete Wedding Planning
                </h3>
              </div>
              <p className="mb-6 text-lg leading-relaxed text-muted-foreground">
                From the first consultation to the final farewell, we manage every aspect of your celebration. Our comprehensive planning services ensure a seamless experience, allowing you to savor every moment while we handle the intricate details.
              </p>
              <ul className="space-y-3 text-muted-foreground">
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>Venue selection and design conceptualization</span>
                </li>
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>Vendor coordination and management</span>
                </li>
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>Timeline creation and day-of coordination</span>
                </li>
              </ul>
            </div>
            <motion.div
              className="order-1 lg:order-2 overflow-hidden rounded-lg"
              whileHover={{ scale: 1.02 }}
              transition={{ duration: 0.3 }}
            >
              <img
                src="/airo-assets/images/pages/home/service-planning"
                alt="Wedding Planning"
                className="h-[500px] w-full object-cover"
              />
            </motion.div>
          </motion.div>

          {/* Service 2 - Venue Selection */}
          <motion.div
            className="grid items-center gap-12 lg:grid-cols-2"
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <motion.div
              className="overflow-hidden rounded-lg"
              whileHover={{ scale: 1.02 }}
              transition={{ duration: 0.3 }}
            >
              <img
                src="/airo-assets/images/pages/home/service-venue"
                alt="Luxury Venues"
                className="h-[500px] w-full object-cover"
              />
            </motion.div>
            <div>
              <div className="flex items-center gap-3 mb-4">
                <MapPin className="h-6 w-6 text-primary" />
                <h3 className="font-heading text-3xl font-light tracking-wide">
                  Destination & Venue Curation
                </h3>
              </div>
              <p className="mb-6 text-lg leading-relaxed text-muted-foreground">
                Whether you envision a palace wedding in Rajasthan, a beachside celebration in Goa, or an international destination, we curate extraordinary venues that reflect your vision and exceed your expectations.
              </p>
              <ul className="space-y-3 text-muted-foreground">
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>Exclusive access to premium venues</span>
                </li>
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>International and domestic destination expertise</span>
                </li>
                <li className="flex items-start">
                  <span className="mr-3 mt-1 h-1.5 w-1.5 rounded-full bg-primary" />
                  <span>Site visits and venue negotiations</span>
                </li>
              </ul>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Gallery Preview - Bento Grid */}
      <section className="bg-muted py-24 lg:py-32">
        <div className="container mx-auto px-6 lg:px-12">
          <motion.div
            className="mb-16 text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <h2 className="mb-4 font-heading text-4xl font-light tracking-wide lg:text-5xl">
              Our Portfolio
            </h2>
            <p className="mx-auto max-w-2xl text-lg text-muted-foreground">
              A glimpse into the celebrations we've brought to life
            </p>
          </motion.div>

          <div className="grid grid-cols-2 gap-4 lg:grid-cols-4 lg:gap-6">
            {[
              { slot: 'gallery-1', span: 'lg:col-span-2 lg:row-span-2' },
              { slot: 'gallery-2', span: 'lg:col-span-2' },
              { slot: 'gallery-3', span: '' },
              { slot: 'gallery-4', span: 'lg:col-span-2' },
              { slot: 'gallery-5', span: '' },
              { slot: 'gallery-6', span: 'lg:col-span-2' },
            ].map((item, index) => (
              <motion.div
                key={item.slot}
                className={`group relative overflow-hidden rounded-lg ${item.span}`}
                initial={{ opacity: 0, scale: 0.9 }}
                whileInView={{ opacity: 1, scale: 1 }}
                viewport={{ once: true }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                whileHover={{ scale: 1.05 }}
              >
                <img
                  src={`/airo-assets/images/pages/home/${item.slot}`}
                  alt={`Gallery ${index + 1}`}
                  className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110"
                />
                <div className="absolute inset-0 bg-gradient-to-t from-gray-900/60 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100" />
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section className="py-24 lg:py-32">
        <div className="container mx-auto px-6 lg:px-12">
          <motion.div
            className="mb-16 text-center"
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <h2 className="mb-4 font-heading text-4xl font-light tracking-wide lg:text-5xl">
              Cherished Memories
            </h2>
            <p className="mx-auto max-w-2xl text-lg text-muted-foreground">
              Words from couples whose dreams we've brought to life
            </p>
          </motion.div>

          <div className="grid gap-8 lg:grid-cols-2">
            <motion.div
              className="rounded-lg border border-border bg-card p-8"
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5 }}
            >
              <div className="mb-6 flex items-center gap-4">
                <img
                  src="/airo-assets/images/pages/home/testimonial-1"
                  alt="Priya & Arjun"
                  className="h-16 w-16 rounded-full object-cover"
                />
                <div>
                  <h4 className="font-heading text-xl font-light">Priya & Arjun</h4>
                  <p className="text-sm text-muted-foreground">Udaipur, December 2025</p>
                </div>
              </div>
              <p className="font-serif italic leading-relaxed text-muted-foreground">
                "HeavenlyWeds transformed our vision into a reality beyond our wildest dreams. Every detail was executed with such precision and elegance. Our three-day celebration at the palace was absolutely magical."
              </p>
            </motion.div>

            <motion.div
              className="rounded-lg border border-border bg-card p-8"
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: 0.1 }}
            >
              <div className="mb-6 flex items-center gap-4">
                <img
                  src="/airo-assets/images/pages/home/testimonial-2"
                  alt="Ananya & Rohan"
                  className="h-16 w-16 rounded-full object-cover"
                />
                <div>
                  <h4 className="font-heading text-xl font-light">Ananya & Rohan</h4>
                  <p className="text-sm text-muted-foreground">Jaipur, November 2025</p>
                </div>
              </div>
              <p className="font-serif italic leading-relaxed text-muted-foreground">
                "From our first meeting to the final send-off, the team was exceptional. They understood our cultural traditions while adding contemporary elegance. Our guests are still talking about it!"
              </p>
            </motion.div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="bg-primary py-24 text-primary-foreground lg:py-32">
        <div className="container mx-auto px-6 text-center lg:px-12">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5 }}
          >
            <Calendar className="mx-auto mb-6 h-12 w-12" />
            <h2 className="mb-4 font-heading text-4xl font-light tracking-wide lg:text-5xl">
              Begin Your Journey
            </h2>
            <p className="mx-auto mb-8 max-w-2xl text-lg opacity-90">
              Let's create an unforgettable celebration that reflects your unique love story.
              Schedule a consultation to start planning your dream wedding.
            </p>
            <Button
              size="lg"
              variant="secondary"
              className="group"
            >
              Schedule Consultation
              <ArrowRight className="ml-2 h-5 w-5 transition-transform group-hover:translate-x-1" />
            </Button>
          </motion.div>
        </div>
      </section>
    </div>
  );
}
